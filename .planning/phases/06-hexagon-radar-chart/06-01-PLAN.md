---
phase: 06-hexagon-radar-chart
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ui/hex-radar.tsx
autonomous: true
requirements:
  - HERO-05
must_haves:
  truths:
    - "HexRadar supports mount animation (scale/opacity from center) when animated=true and user has no reduced-motion preference"
    - "When useReducedMotion() is true, chart renders in final state with no motion timeline"
    - "Each axis vertex exposes a hover/focus tooltip with full axis name and value 0-100"
    - "SVG uses existing design tokens; no new ad-hoc hex colors"
  artifacts:
    - path: "components/ui/hex-radar.tsx"
      provides: "Animated optional radar, Radix tooltips on vertices, a11y preserved"
      exports: ["HexRadar", "AXES", "axisDisplayName helper if added"]
  key_links:
    - from: "TooltipProvider"
      to: "Radix tooltip root"
      via: "wrap svg or outer fragment"
---

<objective>
Upgrade `HexRadar` so it satisfies Phase 6 UI-SPEC for **motion** (draw-on feel) and **interaction** (axis tooltips), without breaking existing consumers (`target` overlay, `variant`, sizes).

Output: updated `components/ui/hex-radar.tsx` only.
</objective>

<execution_context>
@F:/_WORK_/PROJECTS/TEAM-PROJECTS/devraid-front/.planning/phases/06-hexagon-radar-chart/06-RESEARCH.md
@F:/_WORK_/PROJECTS/TEAM-PROJECTS/devraid-front/.planning/phases/06-hexagon-radar-chart/06-UI-SPEC.md
</execution_context>

<context>
@F:/_WORK_/PROJECTS/TEAM-PROJECTS/devraid-front/components/ui/hex-radar.tsx
@F:/_WORK_/PROJECTS/TEAM-PROJECTS/devraid-front/package.json
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add axis display names and optional animated prop</name>
  <files>components/ui/hex-radar.tsx</files>
  <action>
- Import `motion`, `useReducedMotion` from `framer-motion`.
- Import Radix: `Tooltip`, `TooltipContent`, `TooltipProvider`, `TooltipTrigger` from `@radix-ui/react-tooltip`.
- Add exported map `AXIS_LABELS: Record&lt;AxisKey, string&gt;` with full names: Initiative, Expertise, Execution speed, Communication, Flexibility (match UI-SPEC table).
- Extend `HexRadarProps` with `animated?: boolean` (default `true`).
  </action>
  <verify>grep -E "AXIS_LABELS|animated" components/ui/hex-radar.tsx</verify>
  <done>Types compile; defaults preserve current behaviour for `animated` true</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Mount animation with reduced-motion guard</name>
  <files>components/ui/hex-radar.tsx</files>
  <action>
- Read `const reduceMotion = useReducedMotion()`.
- Wrap the **chart content** (grid polygons, axis lines, target overlay if any, hero polygon, vertex dots, labels) in `motion.g`:
  - If `!animated || reduceMotion`: render plain `g` without animation props.
  - Else: `initial={{ opacity: 0.65, scale: 0.2 }}` `animate={{ opacity: 1, scale: 1 }}` `transition={{ duration: 0.75, ease: [0.4, 0, 0.2, 1] }}` with `style={{ transformOrigin: `${cx}px ${cy}px` }}` or equivalent so scaling is from hex center.
- Keep `viewBox` and dimensions stable; do not animate `width`/`height` attributes with random values.
  </action>
  <verify>npm run build</verify>
  <done>Build passes; no TS errors on motion.g</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Radix tooltips on axis vertices</name>
  <files>components/ui/hex-radar.tsx</files>
  <action>
- Wrap the entire SVG return in `TooltipProvider` (delayDuration={200}).
- Replace bare `circle` vertices with `Tooltip` &gt; `TooltipTrigger asChild` wrapping a `circle` with `r` at least 6–8 for hit area (can keep visual r=2.5 via duplicate circles or stroke — prefer single circle with r=8 and fill opacity 0 for hit area + visible dot — simplify: one circle r=6 with fill the axis color).
- `TooltipContent` side="top" className using tokens: background `var(--color-dr-surface-2)`, border `var(--color-dr-edge-2)`, text `var(--color-dr-text)`, font mono, text-sm. Content: `{AXIS_LABELS[key]} — {profile[key]}/100`.
- Ensure `portal` does not break SSR: Radix handles client; file is already `'use client'`.
  </action>
  <verify>npm run build</verify>
  <done>Hover shows tooltip text with value</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Verification pass</name>
  <files>components/ui/hex-radar.tsx</files>
  <action>
Run `npm run build`. Manually sanity: import page using HexRadar still typechecks (e.g. roll-hero-reveal if it imports HexRadar).
  </action>
  <verify>npm run build</verify>
  <done>EXIT 0</done>
</task>

</tasks>

---

## PLAN CHECK (self)

- Aligns with `06-UI-SPEC.md` motion + tooltip + tokens: **yes**
- Atomic scope (one primary file): **yes**
- Dependencies: **none** (wave 1)

**## PLAN COMPLETE**
