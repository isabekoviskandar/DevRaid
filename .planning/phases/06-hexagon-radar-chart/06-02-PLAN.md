---
phase: 06-hexagon-radar-chart
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01-PLAN.md
files_modified:
  - app/hero/me/page.tsx
  - app/hero/[id]/page.tsx
  - lib/mock-data.ts
autonomous: true
requirements:
  - HERO-05
  - HERO-06
must_haves:
  truths:
    - "/hero/me shows Soft skills section: HexRadar when user.soft_skills defined"
    - "/hero/me without soft_skills shows empty card + CTA linking to /onboarding per UI-SPEC copy"
    - "/hero/[id] exists; shows readonly profile with radar when soft_profile present"
    - "/hero/[id] without soft skills shows neutral copy for viewers (not onboarding CTA)"
    - "Unknown hero id returns notFound()"
  artifacts:
    - path: "app/hero/me/page.tsx"
      provides: "Soft skills section integrated"
    - path: "app/hero/[id]/page.tsx"
      provides: "Public hero profile page"
    - path: "lib/mock-data.ts"
      provides: "Helper resolveMockHeroById(id) or equivalent"
  key_links:
    - from: "getUser soft_skills"
      to: "HexRadar profile prop"
      via: "user.soft_skills as SoftSkillHexagon"
---

<objective>
Integrate the upgraded `HexRadar` into **my profile** and add **public hero** route per Phase 6 success criteria and `06-UI-SPEC.md`.

Output: `app/hero/me/page.tsx` updated; **new** `app/hero/[id]/page.tsx`; small mock helper in `lib/mock-data.ts`.
</objective>

<execution_context>
@F:/_WORK_/PROJECTS/TEAM-PROJECTS/devraid-front/.planning/phases/06-hexagon-radar-chart/06-RESEARCH.md
@F:/_WORK_/PROJECTS/TEAM-PROJECTS/devraid-front/.planning/phases/06-hexagon-radar-chart/06-UI-SPEC.md
</execution_context>

<context>
@F:/_WORK_/PROJECTS/TEAM-PROJECTS/devraid-front/app/hero/me/page.tsx
@F:/_WORK_/PROJECTS/TEAM-PROJECTS/devraid-front/lib/mock-data.ts
@F:/_WORK_/PROJECTS/TEAM-PROJECTS/devraid-front/lib/api.ts
@F:/_WORK_/PROJECTS/TEAM-PROJECTS/devraid-front/types/index.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Mock resolver for public hero id</name>
  <files>lib/mock-data.ts</files>
  <action>
Add exported function:

```ts
export function resolveMockHeroProfile(id: string): HeroProfile | null {
  if (id === 'me' || id === MOCK_HERO.id) return MOCK_HERO
  const cand = MOCK_CANDIDATES.find(c => c.hero.id === id)
  return cand ? cand.hero : null
}
```

Adjust if `MOCK_HERO.id` is `'me'` — ensure match works for sample links `c1`..`c5` from `MOCK_CANDIDATES`.
  </action>
  <verify>npm run build</verify>
  <done>Exported function used by hero/[id]</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: /hero/me — Soft skills section</name>
  <files>app/hero/me/page.tsx</files>
  <action>
- Import `HexRadar` from `@/components/ui/hex-radar` and `Link` from `next/link`.
- After the main header card (or after Skills &amp; Profile card per UX — **prefer after Skills &amp; Profile**, before edit panel), add a `.dr-card` section:
  - `h2` title: **Soft skills** with class `dr-heading text-base`.
  - If `user.soft_skills`: render `HexRadar profile={user.soft_skills} size={200}` (or 220) `showLabels` true, `animated` true.
  - Else: use same visual language as other empty states — heading **Map your soft skills**, body one line from UI-SPEC, `Link href="/onboarding"` with classes `dr-btn-primary` (or button-styled link) text **Complete assessment →**.
- Do not show onboarding CTA when user is not authenticated (already gated above).
  </action>
  <verify>npm run build</verify>
  <done>Conditional UI compiles; types match SoftSkillHexagon</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create /hero/[id] page</name>
  <files>app/hero/[id]/page.tsx</files>
  <action>
- New file: Server Component default export.
- `const { id } = await params`
- `const hero = resolveMockHeroProfile(id)` — if null `notFound()`.
- Layout: similar spacing to `/hero/me` — card with `display_name`, `headline`, `bio` (if any), hard_skills chips optional.
- Soft skills: if `hero.soft_profile`, render **client** island: create thin `components/features/hero/public-hero-radar.tsx` `'use client'` that only wraps `HexRadar profile={hero.soft_profile} size={200}` **OR** mark page as client if you keep everything in one file — **prefer small client child** to keep RSC for static parts.
- If no `soft_profile`: text **Soft skills not shared** styled `var(--color-dr-text-2)`.
- No Edit button, no onboarding CTA.
- Add back link to `/raids` or home as appropriate (`dr-label` muted).
  </action>
  <verify>npm run build</verify>
  <done>Route appears in `next build` output as dynamic `/hero/[id]`</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Optional deep link discoverability</name>
  <files>app/raids/[id]/page.tsx</files>
  <action>
If captain/hero names in raid detail already link somewhere, optional: link hero display to `/hero/{id}` for mock candidate ids — **only if trivial** (≤5 lines). If not trivial, skip and note in SUMMARY.
  </action>
  <verify>npm run build</verify>
  <done>Build green</done>
</task>

</tasks>

---

## PLAN CHECK (self)

- Depends on 06-01 (HexRadar tooltips/animation): **yes**
- Covers HERO-05 + HERO-06: **yes**
- Matches UI-SPEC empty vs public copy: **yes**

**## PLAN COMPLETE**
