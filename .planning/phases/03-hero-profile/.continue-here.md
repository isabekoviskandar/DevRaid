---
phase: 03-hero-profile
task: 0
total_tasks: 3
status: ready_to_execute
last_updated: 2026-04-02T10:40:55.331Z
---

<current_state>
Phase 3 полностью спланирована. Plan Checker вернул PASS после фикса draft useState инициализатора.
Готово к выполнению: /gsd:execute-phase 3

Phase 2 завершена (commit 55e0f88). Два баг-фикса применены в той же сессии:
- Потеря фокуса на /register (Field компонент вынесен из рендера)
- Apply Modal без авторизации (auth guard добавлен в Trigger)
</current_state>

<completed_work>

**Phase 2 — COMPLETE:**
- app/captain/new/page.tsx — полная форма рейда (RoleEntry, RaidFormData, STAGE_OPTIONS, dynamic roles, success state)
- components/features/raids/apply-modal.tsx — Client Island с Radix Dialog + Framer Motion + role selector + motivation textarea
- app/raids/[id]/page.tsx — статичная кнопка Apply заменена на ApplyModal island

**Bug fixes (post-Phase 2):**
- app/register/page.tsx — Field компонент вынесен на module-level (фикс потери фокуса)
- components/features/raids/apply-modal.tsx — auth check в onClick trigger (router.push('/login') если не залогинен)

**Phase 3 — PLANNED (не выполнена):**
- Roadmap и REQUIREMENTS.md обновлены: добавлены Phase 3, 4, 5
- 03-RESEARCH.md написан (HIGH confidence): ApiUser missing hard_skills/github_url/linkedin_url, хранить в localStorage
- 03-01-PLAN.md создан и прошёл Plan Checker (PASS после фикса draft initializer)
</completed_work>

<remaining_work>

**Phase 3 — 03-01 (не выполнена):**

- **Task 1**: Extend ApiUser + authStore.updateUser()
  - lib/api.ts: добавить `github_url?`, `linkedin_url?`, `hard_skills?` в ApiUser; patchUser() stub
  - lib/auth-store.ts: добавить updateUser(patch: Partial<ApiUser>) метод

- **Task 2**: Заменить "coming soon" секцию реальным отображением навыков (HERO-02)
  - Chips для hard_skills, иконки GitHub/LinkedIn (lucide-react), empty state

- **Task 3**: Inline edit panel с Framer Motion анимацией (HERO-03, HERO-04)
  - Edit Profile кнопка → AnimatePresence expand panel
  - Bio textarea, GitHub/LinkedIn inputs, tag input для навыков
  - Save → authStore.updateUser() → panel closes after 1.5s "✓ Saved"
</remaining_work>

<decisions_made>

- hard_skills/github_url/linkedin_url хранятся в localStorage (бэк endpoints ещё не готовы)
- draft useState инициализируется через authStore.getUser()?.field ?? '' — НЕ через user (React state null на первом рендере)
- Mock save: authStore.updateUser() + api.auth.patchUser() stub; без тостов (out of scope по PROJECT.md)
- Inline кнопка-состояние: idle → "Saving…" → "✓ Saved" → panel closes
- Для хакатон-демо: если hard_skills пусто, используется empty state (не seed из mock)
- Phases 4 и 5 добавлены в роадмап: Raid Board (фильтры/поиск) и Captain Board (управление заявками)
</decisions_made>

<blockers>
Нет блокеров. Phase 3 готова к выполнению.

Human actions (не блокируют):
- Протестировать Phase 2 баг-фиксы вручную (/register ввод + Apply без авторизации)
</blockers>

<context>
DevRaid — фэнтези IT-платформа. Стек: Next.js 15 + React 19 + Tailwind v4 + Framer Motion + Radix UI. Дизайн "Obsidian Citadel" (тёмный, glassmorphism, gold/neon). Бэкенд Laravel 11 + Sanctum на :8000 — только auth endpoints готовы.

Роадмап:
- Phase 1 ✅ Auth guard + real hero data
- Phase 2 ✅ Captain/new form + Apply Modal
- Phase 3 📋 Hero Profile editing (PLAN READY)
- Phase 4 📋 Raids list + filters (в роадмапе, не спланировано)
- Phase 5 📋 Captain Board — applications (в роадмапе, не спланировано)
</context>

<next_action>
Start with: /gsd:execute-phase 3
Это запустит executor агент для 03-01-PLAN.md (3 tasks, wave 1).
</next_action>
