---
phase: 04-design-system-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - app/page.tsx
  - app/raids/page.tsx
  - app/hero/me/page.tsx
  - components/ui/empty-state.tsx
autonomous: true
requirements:
  - (design, no functional requirements)

must_haves:
  truths:
    - "Primary accent is emerald #00D97E — no blue #5B87FF visible as interactive primary"
    - ".dr-heading class exists in globals.css with Cinzel font, weight 700"
    - "h1 on landing page uses .dr-heading"
    - ".dr-divider-rune class exists with ::after diamond glyph"
    - ".dr-card-interactive class exists with emerald hover ring"
    - "EmptyState component exists at components/ui/empty-state.tsx"
    - "Raids page shows SVG empty state when raids array is empty"
    - "Hard skills section shows SVG empty state when skills array is empty"
    - "TypeScript: tsc --noEmit exits 0"
  artifacts:
    - path: "app/globals.css"
      provides: "Updated tokens + new utility classes"
    - path: "components/ui/empty-state.tsx"
      provides: "Reusable empty state component"
---

<objective>
Refresh the DevRaid design system from cold cyberpunk blue to emerald fantasy dark.

Changes:
1. Replace primary accent #5B87FF → #00D97E (emerald) in all CSS token values and hardcoded rgba
2. Add new utility classes: .dr-heading, .dr-divider-rune, .dr-card-interactive
3. Migrate h1/h2 headings to .dr-heading (Cinzel) on landing, raids, and hero pages
4. Build reusable EmptyState component + SVG illustrations for raids and skills
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-design-system-refresh/04-UI-SPEC.md
@.planning/phases/04-design-system-refresh/04-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update CSS tokens — swap blue accent to emerald</name>
  <files>app/globals.css</files>
  <action>
In `app/globals.css`, make the following targeted changes:

**1. In @theme block — update glow tokens:**
Change:
```css
--color-dr-glow:         #5B87FF;
--color-dr-glow-dim:     #5B87FF1A;
--color-dr-glow-mid:     #5B87FF44;
```
To:
```css
--color-dr-glow:         #00D97E;
--color-dr-glow-dim:     #00D97E1A;
--color-dr-glow-mid:     #00D97E44;
```

**2. In @theme block — update role and axis tokens:**
Change:
```css
--color-dr-role-frontend:   #5B87FF;
--color-dr-status-active:     #5B87FF;
--color-dr-axis-initiative:    #5B87FF;
```
To:
```css
--color-dr-role-frontend:   #00D97E;
--color-dr-status-active:   #00D97E;
--color-dr-axis-initiative: #00D97E;
```

**3. In @theme block — add new tokens** (after `--color-dr-gold-dim`):
```css
  /* ── Emerald aliases (forward-compat for radar chart, empty states) ── */
  --color-dr-emerald:     #00D97E;
  --color-dr-emerald-dim: #00D97E1A;
  --color-dr-emerald-mid: #00D97E44;
  --color-dr-info:        #5B87FF;
  --color-dr-danger:      #EF4444;
```

**4. Update `.dr-aurora` background** — replace both rgba values:
Change `rgba(91,  135, 255, 0.10)` → `rgba(0, 217, 126, 0.10)`

**5. Update `::selection`:**
Change `rgba(91, 135, 255, 0.22)` → `rgba(0, 217, 126, 0.22)`

**6. Update `.dr-glow-ring`:**
Replace all `rgba(91, 135, 255, ...)` → `rgba(0, 217, 126, ...)` (keep same alpha values: 0.4, 0.2, 0.08)

**7. Update `.dr-card:hover` box-shadow:**
Change `rgba(91, 135, 255, 0.07)` → `rgba(0, 217, 126, 0.07)`

**8. Update `.dr-btn-primary` gradient and glow:**
Change background: `linear-gradient(135deg, #5B87FF 0%, #3B65D8 100%)` → `linear-gradient(135deg, #00D97E 0%, #00A85F 100%)`
Change hover box-shadow: replace `rgba(91, 135, 255, ...)` → `rgba(0, 217, 126, ...)` (same alphas)

**9. Update `.dr-text-glow` gradient:**
Change:
```css
background: linear-gradient(
  135deg,
  #B0CCFF 0%,
  var(--color-dr-glow) 55%,
  #3B65D8 100%
);
```
To:
```css
background: linear-gradient(
  135deg,
  #8FFFC9 0%,
  var(--color-dr-glow) 55%,
  #00A85F 100%
);
```
  </action>
  <verify>
    <automated>cd "F:/_WORK_/PROJECTS/TEAM-PROJECTS/devraid-front" && cmd /c npx tsc --noEmit 2>&1</automated>
  </verify>
  <done>
    - No #5B87FF present in token or gradient values (blue remains only in --color-dr-info and role/axis colors that are intentionally non-emerald)
    - --color-dr-emerald, --color-dr-info, --color-dr-danger tokens exist in @theme
    - tsc exits 0
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add new utility classes to globals.css</name>
  <files>app/globals.css</files>
  <action>
Append these three new utility classes to `app/globals.css` (after the existing `.dr-divider` block):

```css
/* Fantasy heading — Cinzel, for h1 and h2 only */
.dr-heading {
  font-family: var(--font-fantasy);
  font-weight: 700;
  letter-spacing: 0.04em;
  line-height: 1.15;
}

/* Section divider with centered diamond accent */
.dr-divider-rune {
  position: relative;
  height: 1px;
  border: none;
  background: linear-gradient(
    90deg,
    transparent 0%,
    rgba(255, 255, 255, 0.07) 20%,
    rgba(255, 255, 255, 0.07) 80%,
    transparent 100%
  );
  margin: 0;
}

.dr-divider-rune::after {
  content: '◆';
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  font-size: 8px;
  color: var(--color-dr-muted);
  background: var(--color-dr-bg);
  padding: 0 4px;
  line-height: 1;
}

/* Interactive card — additive class for clickable cards */
.dr-card-interactive:hover {
  border-color: transparent;
  box-shadow:
    0 1px 0 0 rgba(255, 255, 255, 0.06) inset,
    0 8px 36px rgba(0, 0, 0, 0.65),
    0 0 0 1px rgba(0, 217, 126, 0.18);
}
```
  </action>
  <verify>
    <automated>cd "F:/_WORK_/PROJECTS/TEAM-PROJECTS/devraid-front" && cmd /c npx tsc --noEmit 2>&1</automated>
  </verify>
  <done>
    - .dr-heading class exists in globals.css
    - .dr-divider-rune with ::after diamond exists
    - .dr-card-interactive:hover exists with emerald ring
    - tsc exits 0
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Migrate heading classes on landing and raids pages</name>
  <files>app/page.tsx, app/raids/page.tsx</files>
  <action>
**In `app/page.tsx`:**

1. Change the h1 class from `dr-title-fantasy font-bold leading-none` → `dr-heading leading-none`:
```tsx
// Before:
<h1
  className="dr-title-fantasy font-bold leading-none"
  style={{ fontSize: 'clamp(3.5rem, 9vw, 6rem)' }}
>

// After:
<h1
  className="dr-heading leading-none"
  style={{ fontSize: 'clamp(3.5rem, 9vw, 6rem)' }}
>
```

2. Replace the two hardcoded blue hex wireframe strokes (bottom-left decorative hex):
```tsx
// Before:
stroke="rgba(91,135,255,0.06)"
// After:
stroke="rgba(0,217,126,0.06)"

// Before:
stroke="rgba(91,135,255,0.035)"
// After:
stroke="rgba(0,217,126,0.035)"
```

**In `app/raids/page.tsx`:**

Change the h1 class:
```tsx
// Before:
<h1 className="dr-title-fantasy text-2xl font-bold" style={{ color: 'var(--color-dr-text)' }}>

// After:
<h1 className="dr-heading text-xl" style={{ color: 'var(--color-dr-text)' }}>
```
  </action>
  <verify>
    <automated>cd "F:/_WORK_/PROJECTS/TEAM-PROJECTS/devraid-front" && cmd /c npx tsc --noEmit 2>&1</automated>
  </verify>
  <done>
    - h1 on landing uses dr-heading
    - h1 on raids page uses dr-heading
    - No rgba(91,135,255,...) hardcoded values in page.tsx
    - tsc exits 0
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Build EmptyState component with SVG illustrations</name>
  <files>components/ui/empty-state.tsx</files>
  <action>
Create `components/ui/empty-state.tsx`:

```tsx
interface EmptyStateProps {
  svg: React.ReactNode
  heading: string
  body: string
  cta?: {
    label: string
    onClick?: () => void
    href?: string
  }
}

export function EmptyState({ svg, heading, body, cta }: EmptyStateProps) {
  return (
    <div
      className="dr-enter flex flex-col items-center text-center py-12 px-6 gap-4"
      role="status"
    >
      <div aria-hidden="true" className="mb-2">
        {svg}
      </div>
      <h3
        className="text-base font-semibold"
        style={{ color: 'var(--color-dr-text)' }}
      >
        {heading}
      </h3>
      <p
        className="text-sm max-w-xs"
        style={{ color: 'var(--color-dr-text-2)', lineHeight: 1.6 }}
      >
        {body}
      </p>
      {cta && (
        cta.href ? (
          <a href={cta.href} className="dr-btn-secondary text-sm mt-1">
            {cta.label}
          </a>
        ) : (
          <button onClick={cta.onClick} className="dr-btn-secondary text-sm mt-1">
            {cta.label}
          </button>
        )
      )}
    </div>
  )
}

/* ── Inline SVG illustrations ─────────────────────────── */

export function RaidsEmptySvg() {
  return (
    <svg
      width="96"
      height="96"
      viewBox="0 0 96 96"
      fill="none"
      xmlns="http://www.w3.org/2000/svg"
      aria-hidden="true"
    >
      {/* Shield body */}
      <path
        d="M48 8L16 20v28c0 16 14 28 32 36 18-8 32-20 32-36V20L48 8z"
        fill="#263047"
        stroke="#00D97E"
        strokeWidth="1.5"
        strokeLinejoin="round"
      />
      {/* Shield inner detail */}
      <path
        d="M48 18L24 28v22c0 11 10 20 24 26 14-6 24-15 24-26V28L48 18z"
        fill="none"
        stroke="#00D97E"
        strokeWidth="0.75"
        strokeOpacity="0.4"
        strokeLinejoin="round"
      />
      {/* Crossed swords */}
      <line x1="34" y1="38" x2="62" y2="58" stroke="#00D97E" strokeWidth="1.5" strokeLinecap="round" strokeOpacity="0.8" />
      <line x1="62" y1="38" x2="34" y2="58" stroke="#00D97E" strokeWidth="1.5" strokeLinecap="round" strokeOpacity="0.8" />
    </svg>
  )
}

export function SkillsEmptySvg() {
  return (
    <svg
      width="80"
      height="96"
      viewBox="0 0 80 96"
      fill="none"
      xmlns="http://www.w3.org/2000/svg"
      aria-hidden="true"
    >
      {/* Scroll body */}
      <rect x="12" y="12" width="56" height="72" rx="4" fill="#263047" stroke="#C8943A" strokeWidth="1.5" />
      {/* Scroll top roller */}
      <rect x="8" y="8" width="64" height="12" rx="6" fill="#1C2236" stroke="#C8943A" strokeWidth="1.5" />
      {/* Scroll bottom roller */}
      <rect x="8" y="76" width="64" height="12" rx="6" fill="#1C2236" stroke="#C8943A" strokeWidth="1.5" />
      {/* Text lines on scroll */}
      <line x1="24" y1="34" x2="56" y2="34" stroke="#C8943A" strokeWidth="1" strokeOpacity="0.5" strokeLinecap="round" />
      <line x1="24" y1="44" x2="48" y2="44" stroke="#C8943A" strokeWidth="1" strokeOpacity="0.35" strokeLinecap="round" />
      <line x1="24" y1="54" x2="52" y2="54" stroke="#C8943A" strokeWidth="1" strokeOpacity="0.35" strokeLinecap="round" />
      <line x1="24" y1="64" x2="40" y2="64" stroke="#C8943A" strokeWidth="1" strokeOpacity="0.25" strokeLinecap="round" />
    </svg>
  )
}
```
  </action>
  <verify>
    <automated>cd "F:/_WORK_/PROJECTS/TEAM-PROJECTS/devraid-front" && cmd /c npx tsc --noEmit 2>&1</automated>
  </verify>
  <done>
    - EmptyState component exists and exports EmptyState, RaidsEmptySvg, SkillsEmptySvg
    - No TypeScript errors
    - tsc exits 0
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Integrate EmptyState into raids and hero pages</name>
  <files>app/raids/page.tsx, app/hero/me/page.tsx</files>
  <action>
**In `app/raids/page.tsx`:**

Add import at top:
```tsx
import { EmptyState, RaidsEmptySvg } from '@/components/ui/empty-state'
```

Replace the grid section:
```tsx
// Before:
<div className="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-4">
  {raids.map(raid => (
    <Link key={raid.id} href={`/raids/${raid.id}`} className="block">
      <RaidCard
        raid={raid}
        fitScore={raid.id === '1' ? 91 : raid.id === '4' ? 78 : undefined}
      />
    </Link>
  ))}
</div>

// After:
{raids.length === 0 ? (
  <EmptyState
    svg={<RaidsEmptySvg />}
    heading="No Raids Found"
    body="No active raids match your filters. Check back soon or start your own."
    cta={{ label: 'Create a Raid', href: '/captain/new' }}
  />
) : (
  <div className="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-4">
    {raids.map(raid => (
      <Link key={raid.id} href={`/raids/${raid.id}`} className="block">
        <RaidCard
          raid={raid}
          fitScore={raid.id === '1' ? 91 : raid.id === '4' ? 78 : undefined}
        />
      </Link>
    ))}
  </div>
)}
```

**In `app/hero/me/page.tsx`:**

Add import at top:
```tsx
import { EmptyState, SkillsEmptySvg } from '@/components/ui/empty-state'
```

In the Hard Skills section, replace the empty state text:
```tsx
// Before:
) : (
  <p className="dr-label" style={{ color: 'var(--color-dr-muted)' }}>
    No skills added yet.
  </p>
)}

// After:
) : (
  <EmptyState
    svg={<SkillsEmptySvg />}
    heading="No Skills Added Yet"
    body="Add your stack — heroes with skills get matched to raids faster."
    cta={{ label: 'Edit Profile', onClick: () => setIsEditing(true) }}
  />
)}
```

Also migrate the h2 headings to `.dr-heading`:
```tsx
// "Skills & Profile" heading — change from text-sm font-semibold to dr-heading text-base
<h2 className="dr-heading text-base" style={{ color: 'var(--color-dr-text)' }}>
  Skills &amp; Profile
</h2>

// "Edit Profile" panel heading — same
<h3 className="dr-heading text-base mb-4" style={{ color: 'var(--color-dr-text)' }}>
  Edit Profile
</h3>
```
  </action>
  <verify>
    <automated>cd "F:/_WORK_/PROJECTS/TEAM-PROJECTS/devraid-front" && cmd /c npx tsc --noEmit 2>&1</automated>
  </verify>
  <done>
    - raids/page.tsx shows EmptyState when raids.length === 0
    - hero/me/page.tsx shows EmptyState with SkillsEmptySvg when hard_skills is empty
    - h2 headings on hero/me use dr-heading
    - tsc exits 0
  </done>
</task>

</tasks>

<verification>
Run after all tasks:

```bash
cd "F:/_WORK_/PROJECTS/TEAM-PROJECTS/devraid-front" && cmd /c npx tsc --noEmit
```

Exit code must be 0.

Manual smoke test:
1. `npm run dev` → open localhost:3000
2. Landing: h1 "DevRaid" in Cinzel font, emerald primary button, no blue glow
3. /raids: h1 in Cinzel, filter "All" chip in emerald, cards with emerald hover ring
4. /hero/me: skills section h2 in Cinzel, empty state SVG scroll if no skills
5. Verify no hard blue (#5B87FF) appears as primary interactive color anywhere
</verification>

<success_criteria>
- globals.css: --color-dr-glow = #00D97E (not #5B87FF)
- globals.css: --color-dr-info = #5B87FF exists
- globals.css: .dr-heading class exists (Cinzel, 700)
- globals.css: .dr-divider-rune with ::after diamond exists
- globals.css: .dr-card-interactive:hover with emerald ring exists
- components/ui/empty-state.tsx: EmptyState, RaidsEmptySvg, SkillsEmptySvg exported
- Landing h1 uses .dr-heading
- Raids h1 uses .dr-heading
- Hero/me h2 uses .dr-heading
- Raids page has empty state branch
- Hero/me hard skills has SVG empty state
- tsc --noEmit exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/04-design-system-refresh/04-01-SUMMARY.md` following the summary template.
Update `.planning/STATE.md` to reflect Phase 4 completion.
</output>
