---
phase: 02-raid-actions
task: 0
total_tasks: 2
status: ready_to_execute
last_updated: 2026-04-02T00:21:16.189Z
---

<current_state>
Phase 2 полностью спланирована. Оба плана прошли Plan Checker (PASS). Готово к выполнению.
Phase 1 завершена: auth guard на /captain/new + реальные данные в /hero/me.
Сессия остановлена из-за лимита контекста (78%).
</current_state>

<completed_work>

**Phase 1 — COMPLETE (commit f44a041):**
- app/captain/new/page.tsx — создан с auth guard (useEffect + router.replace('/login') + return null)
- app/hero/me/page.tsx — переписан с реальными данными ApiUser (убран MOCK_HERO)
- TypeScript: tsc --noEmit чисто

**Phase 2 — PLANNED (не выполнена):**
- 02-RESEARCH.md (459 строк) — написан, Radix Dialog уже установлен, паттерны определены
- 02-01-PLAN.md (510 строк) — PASS план для /captain/new формы
- 02-02-PLAN.md (341 строка) — PASS план для Apply Modal
</completed_work>

<remaining_work>

- **02-01**: Реализовать форму создания рейда в app/captain/new/page.tsx
  - RoleEntry/RaidFormData интерфейсы
  - Динамический список ролей (crypto.randomUUID ключи, guard на min 1)
  - Поля: title, mission (textarea), stage (select)
  - Mock submit → success state
  - Obsidian Citadel дизайн (.dr-card, .dr-btn-primary)

- **02-02**: Создать Apply Modal
  - components/features/raids/apply-modal.tsx (новый Client Island)
  - Radix Dialog + Framer Motion AnimatePresence + forceMount
  - Выбор роли из доступных + поле мотивации (min 20 chars)
  - Mock submit → success state ("Quest Application Submitted!")
  - Подключить в app/raids/[id]/page.tsx (Server Component, не добавлять 'use client')
</remaining_work>

<decisions_made>

- router.replace (не push) для auth guard — Back не возвращает на защищённую страницу
- @radix-ui/react-dialog для modal — уже установлен v1.1.6, не нужны новые зависимости
- Framer Motion AnimatePresence + Radix forceMount — обязательная комбинация для exit анимаций
- crypto.randomUUID() для ключей ролей — стабильные ключи при удалении из середины списка
- /raids/[id]/page.tsx остаётся Server Component — ApplyModal как Client Island
</decisions_made>

<blockers>
Нет блокеров. Phase 2 готова к немедленному выполнению.

Human action (не блокирует): протестировать Phase 1 вручную (бэк на :8000, фронт npm run dev).
</blockers>

<context>
DevRaid — фэнтези IT-платформа для формирования команд (Герои + Рейды). Стек: Next.js 15 + React 19 + Tailwind v4 + Framer Motion. Дизайн-система "Obsidian Citadel" (тёмный фон #05070D, glassmorphism, gold/neon). Бэкенд Laravel 11 + Sanctum на :8000 (только auth endpoints готовы, остальное mock).

Оба плана Phase 2 верифицированы и готовы. Выполнять параллельно (wave 1, depends_on: []).
</context>

<next_action>
Start with: /gsd:execute-phase 2
Это запустит оба executor агента параллельно (02-01 и 02-02 в wave 1).
</next_action>
